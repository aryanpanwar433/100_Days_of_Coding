#include<stdio.h>
int main(){
    int l=5,b=10; // l and b belongs to length and breath
    printf("the area of rectangleis %d\n",l*b);
    printf("the perimeter of rectangle %d\n",l+b);
    return 0;
}